[develop]
script_dir=$base/lib/gazebo_gymnasium_bridge
[install]
install_scripts=$base/lib/gazebo_gymnasium_bridge

# flake8/pep257 config lived here (max-line-length, quote style, D-code
# ignores); both are superseded by pyproject.toml's [tool.ruff], which
# covers the same ground under one tool -- see test_ruff.py.
